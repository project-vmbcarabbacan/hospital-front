// src/pages/Users.tsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const Users: React.FC = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Users</Typography>
            {/* Add other user components */}
        </Box>
    );
};

export default Users;
